import { SearchempDirective } from './searchemp.directive';

describe('SearchempDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchempDirective();
    expect(directive).toBeTruthy();
  });
});
